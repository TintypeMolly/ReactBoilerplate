import path from "path";

// put app config only
const PORT = 3000;
const DEFAULT_TITLE = "react-boilerplate";
const DEFAULT_DESCRIPTION = "this description is for search engine";
const FAVICON = path.resolve(__dirname, "../example/favicon.png");

export {
  PORT,
  DEFAULT_TITLE,
  DEFAULT_DESCRIPTION,
  FAVICON,
};
